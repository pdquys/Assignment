spring.application.name=FR.KS.JAVA.SPRINGBOOT.P.L001

# PostgreSQL database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/assignment
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate configuration
# create - creates the database schema
# update - updates the schema without dropping existing data
# validate - validates the schema, does not make changes
# none - no schema management
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# swagger-ui custom path
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# JWT secret key
jwt.secret=MP0g7MRaNPCAty4UWthKLbLPRsoKvN7N7WgxqPRY6DTmHRCZqvKLEkAtvyZ0XLCY
jwt.expiration=3600000
jwt.access-exp-ms=900000
jwt.refresh-exp-ms=604800000


# CORS configuration
cors.allowed-origins=http://localhost:3000,http://localhost:4200,http://localhost:5173
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=Authorization,Content-Type,Accept

# Actuator configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized


